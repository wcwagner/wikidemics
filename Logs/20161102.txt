Installing ipykernel on virtualenv

python -m ipykernel install --user --name quac --display-name "Python 3.4 (quac)"

Looking up sqlite, inorder to index access-logs
.exit to exit

reconfigure python3.4 to work with sqlite
./configure --prefix=/home/william/.local --enable-loadable-sqlite-extensions && make && make altinstall

QUAC 2014 PLOS experiment relies on mrmpi map reduce python library, must install shared library and get python wrapper working


NOTES on index_wiki_data.py:

 # this will return a sorted list of file paths corresponding to the path passed in

if args.year:
    input_files = sorted(glob.glob('</path/to/accesslogs/raw/year/year-month/pagecounts*.gz' % (args.year, args.year, args.month)))

#outputs the sqlite indexes?
output_directory_string = '/panfs/scratch3/vol3/gfairchild/wikipedia/index/%s/%s/%s' #YYYY, MM, DD

#must define a schema file
index_schema = '../data/index_schema.sql'

def process_file(itask, mr):

I will extract the logic from process_file, and run it without worrying about map_reduce

They use mrmpi (MapReduce-MPI), when they call
mr.map(len(input_files), processfiles)
It will pass in an integer(?), and that integer is used to index into the input_files array, where processfile then goes and proccesses that specific file into a sqlite table (?)

